node_modules/
recordings/
*.log
.DS_Store
.claude/settings.local.json
# Local preview scaffolding for the landing page (not needed to build/deploy the site).
.claude/launch.json
.claude/static-server.mjs
*.tgz
# Compiled standalone binaries (built in CI, attached to GitHub Releases).
dist/

# Local MCP configs contain machine-specific absolute paths.
# Copy the .example files and fill in your own path.
.mcp.json
.cursor/mcp.json
